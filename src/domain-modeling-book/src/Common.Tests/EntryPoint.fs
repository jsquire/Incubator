namespace DomainModelingMadeFunctional.Common.Tests
    module EntryPoint =
            
        /// No actions are needed, since this is a class library used for holding tests.  The
        /// entry point is necessary to supress a compiler warning.
        [<EntryPoint>]
        let Main _ = 0